meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  # Telegram-бот использует polling, а не веб-сервер
  # Поэтому containerPort не нужен
  scriptName: app.py
  persistenceMount: /data
