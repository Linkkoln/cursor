"""Роутер для эхо-функциональности - обработка сообщений.

Роутер - это как сортировщик писем на почте.
Когда приходит сообщение, роутер смотрит на него и решает:
"Это обычное сообщение? Тогда вызови функцию echo_handler!"

Представь почтовое отделение: письма приходят, и работник смотрит на адрес
и решает, куда его отправить. Роутер делает то же самое с сообщениями!
"""
import logging
from aiogram import Router
from aiogram.types import Message
from aiogram.exceptions import TelegramNetworkError, TelegramAPIError

from bot.keyboards.common import get_main_menu, get_echo_menu
from bot.services.echo import EchoService
from bot.services.message import MessageService

# Создаём объект для записи логов (дневник)
logger = logging.getLogger(__name__)

# Создаём роутер (сортировщик сообщений)
# Это объект, который будет обрабатывать все входящие сообщения
echo_router = Router()


@echo_router.message(lambda message: message.text == "📝 Эхо")
async def cmd_echo_mode(message: Message) -> None:
    """Обработчик кнопки "Эхо" в меню.
    
    Когда пользователь нажимает кнопку "Эхо", бот переходит в эхо-режим
    и показывает специальное меню.
    """
    try:
        # Получаем текст сообщения из сервиса
        echo_mode_text = MessageService.get_echo_mode_message()
        
        await message.answer(
            echo_mode_text,
            reply_markup=get_echo_menu()
        )
    except (TelegramNetworkError, TelegramAPIError) as e:
        logger.error(f"Не удалось отправить сообщение: {e}")


@echo_router.message(lambda message: message.text == "⬅️ Назад в меню")
async def cmd_back_to_menu(message: Message) -> None:
    """Обработчик кнопки "Назад в меню".
    
    Когда пользователь нажимает эту кнопку, бот возвращается в главное меню.
    """
    try:
        # Получаем текст сообщения из сервиса
        back_text = MessageService.get_back_to_menu_message()
        
        await message.answer(
            back_text,
            reply_markup=get_main_menu()
        )
    except (TelegramNetworkError, TelegramAPIError) as e:
        logger.error(f"Не удалось отправить сообщение: {e}")


@echo_router.message()
async def echo_handler(message: Message) -> None:
    """Обработчик всех остальных сообщений - повторяет их обратно.
    
    Эта функция обрабатывает ВСЕ сообщения, кроме команды /start.
    @echo_router.message() без параметров означает "обработай любое сообщение".
    Это как ловушка: если сообщение не попало в другие функции, оно попадёт сюда.
    """
    try:
        # Используем сервис для обработки сообщения
        # Сервис содержит бизнес-логику, роутер только отправляет ответ
        response_text = EchoService.process_message(message.text)
        
        # Отправляем ответ пользователю вместе с меню
        await message.answer(
            response_text,
            reply_markup=get_echo_menu()
        )
    except TelegramNetworkError as e:
        # Если произошла ошибка сети (нет интернета, медленное соединение, таймаут),
        # записываем ошибку в лог, но не останавливаем бота
        # Бот продолжит работать и попробует обработать следующие сообщения
        logger.error(f"Ошибка сети при отправке сообщения: {e}")
    except TelegramAPIError as e:
        # Если произошла ошибка API Telegram (неправильный токен, бот заблокирован и т.д.),
        # записываем ошибку в лог
        logger.error(f"Ошибка API Telegram: {e}")
    except Exception as e:
        # Если произошла какая-то другая неожиданная ошибка,
        # тоже записываем её в лог
        logger.error(f"Неожиданная ошибка: {e}")
